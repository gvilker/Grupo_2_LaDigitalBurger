<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
    <link rel="stylesheet" type="text/css" href="/css/login.css">
    <script src="https://kit.fontawesome.com/157adaf284.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&family=Tektur&display=swap" rel="stylesheet">
    <title>Login DigitalBurguer</title>
</head>
<body>
    
  <%-include("./partials/header")%>  

   
  <div class="Contenido">
    <div>
      <h1 class="tituloPrinc">DIGITAL REGISTER - CREAR CUENTA</h1>
    </div>
    <form action="/user/register" method="POST" class="form-login" enctype="multipart/form-data">
      <div class="formulario">
        <input type="text" name="name" placeholder="Nombre completo" class="control <%= locals.errors && errors.name ? 'is-invalid' : null %>"
        value="<%= locals.oldData ? oldData.name : null %>">
        <% if (locals.errors && errors.name) { %>
          <div>
            <%= errors.name.msg %>
          </div>         
        <% } %>
      </div>  
  
      <div class="formulario">
        <input type="text" name="alias" placeholder="Nombre de usuario" class="control <%= locals.errors && errors.alias ? 'is-invalid' : null %>"
        value="<%= locals.oldData ? oldData.alias : null %>">
        <% if (locals.errors && errors.alias) { %>
          <div>
            <%= errors.alias.msg %>
          </div>         
        <% } %>
      </div>  

      <div class="formulario">
        <input type="email" name="email" placeholder="Correo electrónico" class="control <%= locals.errors && errors.email ? 'is-invalid' : null %>"
        value="<%= locals.oldData ? oldData.email : null %>">
        <% if (locals.errors && errors.email) { %>
          <div>
            <%= errors.email.msg %>
          </div>         
        <% } %>
      </div>  
 
      <div class="formulario">
        <input type="file" name="avatar" class="control">
        <% if (locals.errors && errors.avatar) { %>
          <div>
            <%= errors.avatar.msg %>
          </div>         
        <% } %>
      </div> 

      <div class="formulario">
        <input type="password" name="password" placeholder="Contraseña" class="control <%= locals.errors && errors.password ? 'is-invalid' : null %>">
        <% if (locals.errors && errors.password) { %>
          <div>
            <%= errors.password.msg %>
          </div>         
        <% } %>
      </div> 
  
      <div class="formulario">
        <input type="password" name="confirm_password" placeholder="Confirmar contraseña" class="control <%= locals.errors && errors.confirm_password ? 'is-invalid' : null %>">
        <% if (locals.errors && errors.confirm_password) { %>
          <div>
            <%= errors.confirm_password.msg %>
          </div>         
        <% } %>
      </div> 
  
      <div>
        <button type="submit" class="boton-formulario">Registrarse</button>
      </div>
  
      <div>
        <p>¿Ya tienes una cuenta? <a href="/user/login">Inicia sesión aquí</a></p>
      </div>
    </form>
  </div>
  <%-include ('./partials/footer') %>
</body>
</html>