<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
    <link rel="stylesheet" type="text/css" href="/css/login.css">
    <script src="https://kit.fontawesome.com/157adaf284.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&family=Tektur&display=swap" rel="stylesheet">
    <title>Login DigitalBurguer</title>
</head>
<body>
    
  <%-include("./partials/header")%>  

   
  <div class="Contenido">
    <div>
      <h1 class="tituloPrinc">DIGITAL REGISTER - CREAR CUENTA</h1>
    </div>
    <form action="/user/register" method="POST" class="form-login" enctype="multipart/form-data">
      <div class="formulario">
        <input type="text" name="nombre_completo" placeholder="Nombre completo" class="control <%= locals.errors && errors.nombre_completo ? 'is-invalid' : null %>"
        value="<%= locals.oldData ? oldData.nombre_completo : null %>">
        <% if (locals.errors && errors.nombre_completo) { %>
          <div>
            <%= errors.nombre_completo.msg %>
          </div>         
        <% } %>
      </div>  
  
      <div class="formulario">
        <input type="text" name="nombre_usuario" placeholder="Nombre de usuario" class="control <%= locals.errors && errors.nombre_usuario ? 'is-invalid' : null %>"
        value="<%= locals.oldData ? oldData.nombre_usuario : null %>">
        <% if (locals.errors && errors.nombre_usuario) { %>
          <div>
            <%= errors.nombre_usuario.msg %>
          </div>         
        <% } %>
      </div>  

      <div class="formulario">
        <input type="email" name="correo_electronico" placeholder="Correo electrónico" class="control <%= locals.errors && errors.correo_electronico ? 'is-invalid' : null %>"
        value="<%= locals.oldData ? oldData.correo_electronico : null %>">
        <% if (locals.errors && errors.correo_electronico) { %>
          <div>
            <%= errors.correo_electronico.msg %>
          </div>         
        <% } %>
      </div>  
 
      <div class="formulario">
        <input type="file" name="avatar" class="control">
        <% if (locals.errors && errors.avatar) { %>
          <div>
            <%= errors.avatar.msg %>
          </div>         
        <% } %>
      </div> 

      <div class="formulario">
        <input type="password" name="contrasena" placeholder="Contraseña" class="control <%= locals.errors && errors.contrasena ? 'is-invalid' : null %>">
        <% if (locals.errors && errors.contrasena) { %>
          <div>
            <%= errors.contrasena.msg %>
          </div>         
        <% } %>
      </div> 
  
      <div class="formulario">
        <input type="password" name="confirmar_contrasena" placeholder="Confirmar contraseña" class="control <%= locals.errors && errors.confirmar_contrasena ? 'is-invalid' : null %>">
        <% if (locals.errors && errors.confirmar_contrasena) { %>
          <div>
            <%= errors.confirmar_contrasena.msg %>
          </div>         
        <% } %>
      </div> 
  
      <div>
        <button type="submit" class="boton-formulario">Registrarse</button>
      </div>
  
      <div>
        <p>¿Ya tienes una cuenta? <a href="/user/login">Inicia sesión aquí</a></p>
      </div>
    </form>
  </div>
  <%-include ('./partials/footer') %>
</body>
</html>