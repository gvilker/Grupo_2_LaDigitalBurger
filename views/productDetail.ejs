<!DOCTYPE html>
<html lang="es"></html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" type="text/css" href="/css/styles.css">
  <link rel="stylesheet" type="text/css" href="/css/productoDetail.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<!-- VINCULACION A GOOLE FONTS Y FONTS AWESOME-->
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&family=Tektur&display=swap" rel="stylesheet">
<script src="https://kit.fontawesome.com/157adaf284.js" crossorigin="anonymous"></script>
  <title>DigitalBurguer</title>
</head>

<body>
    <%-include("./partials/header")%>

    <body>
        <h1 class="titulo">DETALLE DEL PRODUCTO</h1>
        <section id="detalle-producto">
            <div class="contenedor-detalle">
            <div class="producto">
                <img class="fotopedido" src="<%= product.imagen %>" alt="<%= product.nombre %>">
                <div class="descripcion">
                    <h2 class="burger"><%= product.nombre %></h2>
                    <p><%= product.descripcion %></p>
                    <p>Precio: $<%= product.precio.toFixed(2) %></p>
                    <p>Calorías por porción: <%= product.calorias %> kcal</p>
                    <p>Grasas por porción: <%= product.grasas %> g</p>
                    <p>Proteínas por porción: <%= product.proteinas %> g</p>
                    <p>Carbohidratos por porción: <%= product.carbohidratos %> g</p>
                    <p>Tamaño de la hamburguesa: <%= product.tamano %> g</p>
                    <p>Ingredientes adicionales: <%= product.ingredientesAdicionales.join(', ') %></p>
                    <p>Nivel de picante: <%= product.picante ? 'Picante' : 'No picante' %></p>
                    <p>Sugerencias de acompañamientos: <%= product.sugerenciasAcompanamiento.join(', ') %></p>
                    <p>Información adicional: <%= product.informacionAdicional %></p>
                    <a href="/products/<%= product.id %>/edit" class="ver-editar">Editar producto</a>
                    <br></br>
                    <a href="#" class="ver-detalle">Añadir al carrito</a>
                </div>
            </div>
        </div>
        </section>
        <section id="content-eliminar-modificar">
            <br></br>
            
        
            <form action="/products/<%= product.id %>/delete" method="post">
                <button type="submit" class="ver-eliminar">Eliminar producto</button>
            </form>
        </section>
    </body>

    <%-include ('./partials/footer') %>
</body>
</html>